<Window x:Class="TKer.Views.Dialogs.CategoryTemplateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="テンプレートからカテゴリーを一括作成" Height="600" Width="520"
        Background="{StaticResource BgCardBrush}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{x:Static SystemCommands.CloseWindowCommand}"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- タイトルバー -->
        <Border Grid.Row="0" Background="{StaticResource BgSecondaryBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel>
                <Button DockPanel.Dock="Right" Width="44" Height="36"
                        Style="{StaticResource WinCloseBtn}"
                        Command="{x:Static SystemCommands.CloseWindowCommand}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Data="{StaticResource Bi.XLg}" Style="{StaticResource BiIcon}"
                          Fill="{StaticResource TextSecondaryBrush}"/>
                </Button>
                <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Title}"
                           FontFamily="Yu Gothic UI" FontWeight="SemiBold" FontSize="13"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           VerticalAlignment="Center" Margin="16,0,0,0"/>
            </DockPanel>
        </Border>

        <!-- コンテンツ -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- テンプレート選択 -->
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="テンプレート" Style="{StaticResource FormLabel}"/>
                <ComboBox x:Name="CmbTemplate" Style="{StaticResource DarkComboBox}"
                          SelectionChanged="CmbTemplate_SelectionChanged"/>
            </StackPanel>

            <!-- カテゴリーリスト（チェックボックス） -->
            <Border Grid.Row="1" Background="{StaticResource BgSecondaryBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    CornerRadius="6" Padding="4">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="CategoryItems">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="8,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}"
                                              VerticalAlignment="Center"/>
                                    <Border Grid.Column="2" Width="16" Height="16"
                                            CornerRadius="3" VerticalAlignment="Center">
                                        <Border.Background>
                                            <SolidColorBrush Color="{Binding ColorBrushColor}"/>
                                        </Border.Background>
                                    </Border>
                                    <TextBlock Grid.Column="4" Text="{Binding Name}"
                                               FontFamily="Yu Gothic UI" FontSize="13"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- ボタン行 -->
            <DockPanel Grid.Row="2" Margin="0,16,0,0">
                <Button DockPanel.Dock="Left" Content="すべて選択" Style="{StaticResource SecondaryButton}"
                        Padding="10,5" FontSize="11" Click="SelectAll_Click"/>
                <Button DockPanel.Dock="Left" Content="すべて解除" Style="{StaticResource SecondaryButton}"
                        Padding="10,5" FontSize="11" Margin="8,0,0,0" Click="DeselectAll_Click"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="キャンセル" Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0" Click="Cancel_Click"/>
                    <Button Content="一括作成" Style="{StaticResource PrimaryButton}"
                            Click="OK_Click"/>
                </StackPanel>
            </DockPanel>
        </Grid>
    </Grid>
</Window>

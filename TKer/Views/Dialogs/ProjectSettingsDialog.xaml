<Window x:Class="TKer.Views.Dialogs.ProjectSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="プロジェクト設定" Height="420" Width="480"
        Background="{StaticResource BgCardBrush}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{x:Static SystemCommands.CloseWindowCommand}"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- タイトルバー -->
        <Border Grid.Row="0" Background="{StaticResource BgSecondaryBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel>
                <Button DockPanel.Dock="Right" Width="44" Height="36"
                        Style="{StaticResource WinCloseBtn}"
                        Command="{x:Static SystemCommands.CloseWindowCommand}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Data="{StaticResource Bi.XLg}" Style="{StaticResource BiIcon}"
                          Fill="{StaticResource TextSecondaryBrush}"/>
                </Button>
                <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Title}"
                           FontFamily="Yu Gothic UI" FontWeight="SemiBold" FontSize="13"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           VerticalAlignment="Center" Margin="16,0,0,0"/>
            </DockPanel>
        </Border>

        <!-- コンテンツ -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="28">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Path Data="{StaticResource Bi.GearFill}" Style="{StaticResource BiIcon}"
                          Fill="{StaticResource TextPrimaryBrush}" Margin="0,0,8,0"/>
                    <TextBlock Text="プロジェクト設定" FontFamily="Yu Gothic UI" FontWeight="Bold"
                               FontSize="18" Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="プロジェクト名 *" Style="{StaticResource FormLabel}"/>
                <TextBox x:Name="TxtName" Style="{StaticResource DarkTextBox}" Margin="0,0,0,12"/>

                <TextBlock Text="説明" Style="{StaticResource FormLabel}"/>
                <TextBox x:Name="TxtDescription" Style="{StaticResource DarkTextBox}"
                         Height="70" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,12"/>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="バージョン" Style="{StaticResource FormLabel}"/>
                        <TextBox x:Name="TxtVersion" Style="{StaticResource DarkTextBox}"
                                 Tag="1.0.0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="PM / 管理者" Style="{StaticResource FormLabel}"/>
                        <TextBox x:Name="TxtManager" Style="{StaticResource DarkTextBox}"
                                 Tag="担当者名"/>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Margin="0,0,8,0" Content="キャンセル" Style="{StaticResource SecondaryButton}" Click="Cancel_Click"/>
                    <Button Margin="0,0,8,0" Content="保存" Style="{StaticResource PrimaryButton}" Click="OK_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>

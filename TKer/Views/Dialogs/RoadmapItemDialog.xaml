<Window x:Class="TKer.Views.Dialogs.RoadmapItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="ロードマップ項目"
        Width="500" SizeToContent="Height"
        Background="{StaticResource BgCardBrush}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{x:Static SystemCommands.CloseWindowCommand}"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトルバー -->
        <Border Grid.Row="0" Background="{StaticResource BgSecondaryBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel>
                <Button DockPanel.Dock="Right" Width="44" Height="36"
                        Style="{StaticResource WinCloseBtn}"
                        Command="{x:Static SystemCommands.CloseWindowCommand}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Data="{StaticResource Bi.XLg}" Style="{StaticResource BiIcon}"
                          Fill="{StaticResource TextSecondaryBrush}"/>
                </Button>
                <TextBlock x:Name="TitleBarText" Text="ロードマップ項目"
                           FontFamily="Yu Gothic UI" FontWeight="SemiBold" FontSize="13"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           VerticalAlignment="Center" Margin="16,0,0,0"/>
            </DockPanel>
        </Border>

        <!-- 入力フォーム -->
        <StackPanel Grid.Row="1" Margin="28,20,28,12">
            <TextBlock x:Name="HeaderTitle" Text="ロードマップ項目を追加"
                       FontFamily="Yu Gothic UI" FontWeight="Bold" FontSize="16"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,16"/>

            <TextBlock Text="タイトル *" FontSize="12"
                       Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtTitle" Style="{StaticResource DarkTextBox}"
                     Margin="0,0,0,12"/>

            <TextBlock Text="バージョン / 期間 *" FontSize="12"
                       Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtVersion" Style="{StaticResource DarkTextBox}"
                     Tag="例: v1.1.0  /  2026 Q2"
                     Margin="0,0,0,12"/>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="ステータス" FontSize="12"
                               Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,4"/>
                    <ComboBox x:Name="CmbStatus" Style="{StaticResource DarkComboBox}"
                              Height="36" Padding="10,0"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="目標日 (yyyy/MM/dd)" FontSize="12"
                               Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,4"/>
                    <TextBox x:Name="TxtTargetDate" Style="{StaticResource DarkTextBox}"
                             Tag="yyyy/MM/dd"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="説明" FontSize="12"
                       Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtDescription" Style="{StaticResource DarkTextBox}"
                     Height="92" AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalContentAlignment="Top"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <!-- ボタン -->
        <Border Grid.Row="2" Padding="20,0,20,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="キャンセル" Style="{StaticResource SecondaryButton}"
                        Padding="14,6" Margin="0,0,8,0"
                        IsCancel="True" Click="Cancel_Click"/>
                <Button x:Name="BtnOk" Content="作成"
                        Style="{StaticResource PrimaryButton}"
                        Padding="22,6" IsDefault="True"
                        Click="Ok_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

<Window x:Class="TKer.Views.Dialogs.TableColumnDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="テーブル定義" Height="560" Width="520"
        Background="{StaticResource BgCardBrush}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{x:Static SystemCommands.CloseWindowCommand}"/>
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトルバー -->
        <Border Grid.Row="0" Background="{StaticResource BgSecondaryBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel>
                <Button DockPanel.Dock="Right" Width="44" Height="36"
                        Style="{StaticResource WinCloseBtn}"
                        Command="{x:Static SystemCommands.CloseWindowCommand}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Data="{StaticResource Bi.XLg}" Style="{StaticResource BiIcon}"
                          Fill="{StaticResource TextSecondaryBrush}"/>
                </Button>
                <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Title}"
                           FontFamily="Yu Gothic UI" FontWeight="SemiBold" FontSize="13"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           VerticalAlignment="Center" Margin="16,0,0,0"/>
            </DockPanel>
        </Border>

        <!-- コンテンツ -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <!-- テーブル名 -->
                <TextBlock Text="テーブル名" Foreground="{StaticResource TextDimBrush}"
                           FontSize="12" Margin="0,0,0,4"/>
                <TextBox x:Name="TxtTableName" Style="{StaticResource DarkTextBox}"
                         Margin="0,0,0,20" FontSize="14"/>

                <!-- カラム定義 -->
                <DockPanel Margin="0,0,0,8">
                    <TextBlock Text="列定義" FontSize="13" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                    <Button Content="＋ 列を追加" DockPanel.Dock="Right"
                            Style="{StaticResource SecondaryButton}"
                            Padding="10,4,10,4" Click="AddColumn_Click"/>
                </DockPanel>

                <!-- ヘッダー行 -->
                <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="36"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="列名" Foreground="{StaticResource TextDimBrush}" FontSize="11" Margin="4,0"/>
                    <TextBlock Grid.Column="1" Text="型" Foreground="{StaticResource TextDimBrush}" FontSize="11" Margin="4,0"/>
                </Grid>

                <ItemsControl x:Name="ColumnList"/>

                <TextBlock x:Name="NoColumnText" Text="列がありません。「＋ 列を追加」で追加してください。"
                           Foreground="{StaticResource TextDimBrush}" FontSize="12"
                           Margin="0,8,0,0" Visibility="Visible"/>
            </StackPanel>
        </ScrollViewer>

        <!-- フッター -->
        <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                Background="{StaticResource BgSecondaryBrush}" Padding="24,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="キャンセル" Style="{StaticResource SecondaryButton}"
                        Padding="16,6" Margin="0,0,8,0" Click="Cancel_Click"/>
                <Button Content="保存" Style="{StaticResource PrimaryButton}"
                        Padding="20,6" Click="Save_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
